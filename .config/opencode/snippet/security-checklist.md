Security checklist:
- Never commit secrets or .env files
- Validate all user input
- Use parameterized queries for DB operations
- Apply least-privilege principle for IAM roles
- Scan IaC with Trivy before deploying
- Ensure TLS everywhere, no plaintext configs
